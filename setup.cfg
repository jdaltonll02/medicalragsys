[metadata]
name = medical-rag-system
version = 0.1.0
description = Medical RAG system (editable install for local development)

[options]
packages = find:
package_dir =
    = .
python_requires = >=3.10

[options.packages.find]
include = src, src.*
where = .
